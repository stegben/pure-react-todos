import React, { Component, PropTypes } from 'react';

class TodoItem extends Component {
  render() {
    return (
      <div>

      </div>
    );
  }
}

TodoItem.propTypes = {

};

export default TodoItem;
